version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: porfo-arkane
    ports:
      - "3002:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: always
